services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: matchintel
    ports:
      - "5432:5432"
    volumes:
      - matchintel-postgres:/var/lib/postgresql/data
volumes:
  matchintel-postgres:
